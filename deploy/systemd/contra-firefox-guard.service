[Unit]
Description=Contra Firefox Guard
After=network.target

[Service]
Type=simple
EnvironmentFile=-/etc/contra/contra-firefox-guard.env
ExecStart=/usr/bin/env bash __CONTRA_REPO_SCRIPTS_DIR__/contra-firefox-guard.sh --daemon
Restart=always
RestartSec=1

[Install]
WantedBy=multi-user.target
